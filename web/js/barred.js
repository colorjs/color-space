// Proprietary catalog systems (Pantone, NCS, RAL Classic, HKS, Toyo, British Standard,
// DIN 6164) were removed — swatch books, not color spaces, out of scope (see README
// "What it isn't"). Empty export kept so the page's now-inert barred wiring resolves.
export default []
